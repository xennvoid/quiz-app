import { FC } from 'react';

interface HomeProps {}

const Home: FC<HomeProps> = ({}) => {
    return (
        <>
            <h1 className="text-5xl font-bold text-red-600">Quiz App</h1>
            <span>123</span>
        </>
    );
};

export default Home;
